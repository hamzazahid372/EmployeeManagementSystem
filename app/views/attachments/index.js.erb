$("#documents-tab-content").html("<%= j render(partial: 'table', locals: { attachments: @attachments }) %>");
$('#attachments-datatable').DataTable({
    info: false,
    paging: false,
    searching: false
});
