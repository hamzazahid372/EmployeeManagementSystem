$("#events-table-div").html("<%= j render(partial: 'table', locals: { paginate: true, events: @events }) %>");
$('#events-datatable').DataTable({
    info: false,
    paging: false,
    searching: false
  });
