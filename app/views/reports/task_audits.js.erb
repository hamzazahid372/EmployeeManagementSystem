$("#history-tab-content").html("<%= j render(partial: 'reports/history', locals: { task: @task }) %>");
$('#history-datatable').DataTable({
    info: false,
    paging: false,
    searching: false
});


