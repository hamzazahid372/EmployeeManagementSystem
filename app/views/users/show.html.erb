<%= javascript_pack_tag 'comment', 'data-turbolinks-track': 'reload' %>
<%= javascript_pack_tag 'attachment', 'data-turbolinks-track': 'reload' %>
<br><br>

<% content_for :card_header do %>
  <div class="card-header">
    <h2 class="font-weight-bold float-left">
    <%= @user.full_name %>
    </h2>
    <div class="btn-group float-right pt-1" role="group">
      <%= button_to 'Delete', user_path(@user), method: :delete, class: 'btn btn-dark ml-3', data: { confirm: 'Are you sure you want to delete?' } %>
      <%= button_to 'Update', edit_user_path(@user), method: 'get', class: 'btn btn-dark ml-3' %>
    </div>
  </div>
<% end %>

<% presenter = UserPresenter.new(@user, self) %>
<table class="table">
  <tbody>
    <tr>
      <th>First Name</th>
      <td><%= @user.first_name %></td>
    </tr>
    <tr>
      <th>Last Name</th>
      <td><%= @user.last_name%></td>
    </tr>
    <tr>
      <th>Email</th>
      <td><%= @user.email%></td>
    </tr>
    <tr>
      <th>Department</th>
      <td><%= presenter.department %></td>
    </tr>
    <tr>
      <th>Role</th>
      <td><%= presenter.role %></td>
    </tr>
  </tbody>
</table>
<div class="modal fade" id="attachment-modal" role="dialog"></div>
  </tbody>
</table>
<%= render partial: 'shared/tabs', locals: {resource: :user} %>
