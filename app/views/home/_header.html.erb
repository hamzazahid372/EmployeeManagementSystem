<nav class="navbar navbar-dark bg-dark">
  <div  class="w-100">
    <div class="float-left">
      <% if current_user.present? %>
        <button class="navbar-toggler float-left mr-2 navbar-toggler-icon p-3 m-1" onclick="toggleSidebar()"></button>
      <% end %>
      <%= link_to APP_NAME, root_url, class: 'navbar-brand' %>
    </div>    
      <% if current_user.nil? %>
        <%= button_to "Sign Up", new_user_registration_path, method: 'get', class: 'btn btn-outline-light my-2 my-sm-0 float-right'%>
        <%= button_to "Log In", user_companies_find_path, method: 'get', class: 'btn btn-outline-light my-2 my-sm-0 float-right mr-3'%>
      <% else %>
        <div class="float-right">
          <div class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Hi, <%=current_user.first_name%>!</a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to "View Profile", user_path(current_user), method: 'get', class: 'dropdown-item'%>
              <div class="dropdown-divider"></div>
              <%= link_to "Log Out", destroy_user_session_path, method: 'delete', class: 'dropdown-item'%>
            </div>
          </div>
        </div>  
      <% end %>
  </div>
</nav>
