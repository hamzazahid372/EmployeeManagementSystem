$("#tasks-table-div").html("<%= j render(partial: 'table', locals: { paginate: true, tasks: @tasks }) %>");
$('#tasks-datatable').DataTable({
    info: false,
    paging: false,
    searching: false
  });
