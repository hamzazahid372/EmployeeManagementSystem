$("#table-parent-div").html("<%= j render(partial: 'table', locals: { paginate: true, tasks: @tasks }) %>");
$('#tasks_table').DataTable({
    info: false,
    paging: false,
    searching: false
  });
