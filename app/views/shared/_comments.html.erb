</br>
<ul class="nav nav-tabs">
    <li class="nav-item">
        <a href="#" class="nav-link active">Comments</a>
    </li>
</ul>
</br>
<%= form_with url: user_comments_path(object), local: true  do |form| %>
  <div class='container'>
    <div class='col-lg-12'>
      <div class='form-group'>
      <%= form.label :add_comment, class: 'h3 text-dark' %>
      <%= form.text_area :content, class: 'form-control border-dark w-100', rows: 5, required: true %>
    </div>
    <div class="actions text-center m-2 float-right">
      <% params[:commentable_id], params[:commentable_type] = commentable.id, commentable.class %>
      <%= form.submit "Add comment", class: 'btn btn-dark' %>
    </div>
  </div>
<% binding.pry %>


<% end %>