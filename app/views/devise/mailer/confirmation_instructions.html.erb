<p>Welcome <%= @email %>!</p>

<p>Your system generated password is: <%= @resource.sys_generated_password %></p>
<p>You can confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token, subdomain: @resource.company.subdomain) %></p>
