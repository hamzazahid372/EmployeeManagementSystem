$("#users-tab-content").addClass('m-4').html("<%= j render partial: 'users_teams/table', locals: { team: @team, users_teams: @users_teams } %>")
$('#users_teams-datatable').DataTable({
    info: false,
    paging: false,
    searching: false
});